<!-- components/item-card/item-card.wxml -->
<view class="item-card {{item.status === 'expired' ? 'is-expired' : ''}}" bindtap="onTap" bindlongpress="onLongPress">
  <view class="item-icon">
    <text class="icon-emoji">{{catIcon}}</text>
  </view>
  <view class="item-body">
    <view class="item-header">
      <text class="item-name text-ellipsis">{{item.name}}</text>
      <text wx:if="{{tag}}" class="tag {{tag.class}}">{{tag.text}}</text>
    </view>
    <view class="item-meta">
      <text wx:if="{{showSpace && spaceName}}" class="meta-text">📍 {{spaceName}}</text>
      <text wx:if="{{item.brand}}" class="meta-text">{{item.brand}}</text>
      <text wx:if="{{item.quantity > 1}}" class="meta-text">×{{item.quantity}}</text>
      <text wx:if="{{item.expire_date}}" class="meta-text {{item.status === 'expired' ? 'text-danger' : (item.status === 'expiring_soon' ? 'text-warning' : '')}}">📅 {{item.expire_date}}</text>
    </view>
  </view>
</view>
